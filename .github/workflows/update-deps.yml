name: Auto Update Dependencies

on:
  schedule:
    - cron: "0 0 * * *" # Runs every day at midnight UTC
  workflow_dispatch:

permissions:
  contents: write # Needed for pushing changes

jobs:
  update-deps:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: web # Run everything inside /web

    steps:
      # 1. Checkout repository
      - name: Checkout
        uses: actions/checkout@v4

      # 2. Setup Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22.11.0
          cache: "yarn"
          cache-dependency-path: web/yarn.lock

      # 3. Install npm-check-updates
      - name: Install npm-check-updates
        run: yarn global add npm-check-updates

      # 4. Update dependencies in package.json
      - name: Update package.json dependencies
        run: npx npm-check-updates -u

      # 5. Install updated dependencies
      - name: Install updated dependencies
        run: yarn install --frozen-lockfile

      # 6. Commit & push changes
      - name: Commit and Push changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add package.json yarn.lock
          if git commit -m "chore: update dependencies"; then
            git push https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }} HEAD:${{ github.ref_name }}
          else
            echo "No changes to commit"
          fi
